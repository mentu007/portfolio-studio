---
import SiteFooter from '../components/SiteFooter.astro';
import { siteProfile } from '../data/site';
import '../styles/global.css';
import { withBase } from '../utils/paths';

interface Props {
  title?: string;
  description?: string;
  image?: string;
}

const {
  title = siteProfile.seo.title,
  description = siteProfile.seo.description,
  image
} = Astro.props;
const pageTitle = title === siteProfile.seo.title ? title : `${title} | ${siteProfile.seo.title}`;
const canonical = new URL(Astro.url.pathname, Astro.site ?? Astro.url).toString();
const socialImage = image ? new URL(image, Astro.site ?? Astro.url).toString() : undefined;
---

<!doctype html>
<html lang="zh-CN" data-locale="zh" data-theme="light">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <title>{pageTitle}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <link rel="icon" type="image/svg+xml" href={withBase('/favicon.svg')} />
    <link rel="icon" href={withBase('/favicon.ico')} />
    <link rel="preconnect" href={Astro.site?.origin ?? 'https://example.github.io'} />
    <link rel="dns-prefetch" href={Astro.site?.origin ?? 'https://example.github.io'} />
    <meta property="og:type" content="website" />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    {socialImage && <meta property="og:image" content={socialImage} />}
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={pageTitle} />
    <meta name="twitter:description" content={description} />
    {socialImage && <meta name="twitter:image" content={socialImage} />}
    <slot name="head" />
    <script is:inline>
      try {
        const locale = localStorage.getItem('portfolio-locale') === 'en' ? 'en' : 'zh';
        const storedTheme = localStorage.getItem('portfolio-theme');
        const theme =
          storedTheme === 'light' || storedTheme === 'dark'
            ? storedTheme
            : window.matchMedia('(prefers-color-scheme: dark)').matches
              ? 'dark'
              : 'light';
        document.documentElement.dataset.locale = locale;
        document.documentElement.lang = locale === 'en' ? 'en-US' : 'zh-CN';
        document.documentElement.dataset.theme = theme;
        document.documentElement.classList.toggle('dark', theme === 'dark');

        const sidebarWidth = 304;
        const sidebarOffset = window.innerWidth >= 1180 ? `${sidebarWidth + 28}px` : '0px';
        document.documentElement.style.setProperty('--sidebar-offset', sidebarOffset);
      } catch {
        document.documentElement.dataset.locale = 'zh';
        document.documentElement.dataset.theme = 'light';
        document.documentElement.style.setProperty('--sidebar-offset', '0px');
      }
    </script>
  </head>
  <body>
    <div aria-hidden="true" class="pointer-events-none fixed inset-0 -z-10 overflow-hidden">
      <div class="page-background absolute inset-0"></div>
    </div>

    <main class="site-frame">
      <slot />
      <SiteFooter />
    </main>
  </body>
</html>
